IPADDR=$(facter ipaddress_<%= br_ex %>)
if [ "$IPADDR" = '' ]; then
    IPADDR=$(facter ipaddress_br_ex)
fi

cp -f /etc/nova/.nova.conf.puppet /etc/nova/nova.conf
sed -i "s/^vncserver_proxyclient_address.*$/vncserver_proxyclient_address=$IPADDR/g" /etc/nova/nova.conf
